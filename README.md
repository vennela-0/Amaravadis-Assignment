# Amaravadis-Assignment
An offline-first cross-platform Task Manager for Android, iOS and Web. Tasks are stored locally in SQLite, connectivity is detected with the Capacitor Network plugin, and a SyncService simulates pushing pending changes to a backend when the device comes back online (no real API/server).
